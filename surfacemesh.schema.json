{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Format for a surface mesh file",
	"description": "Description of a surface mesh with nodes.\nSchema on github: https://github.com/Engineer2B/PowerCalculator-Schemas",
	"type": "object",
	"required": [
		"Nodes",
		"ElementIndices",
		"Medium",
		"Color"
	],
	"properties": {
		"Nodes": {
			"description": "The coordinates in space where the vertices/nodes of the mesh are located.",
			"type": "array",
			"uniqueItems": true,
			"items": {
				"type": "array",
				"minItems": 3,
				"maxItems": 3,
				"items": {
					"type": "number"
				}
			}
		},
		"ElementIndices": {
			"description": "Array of three node indices which together make up a triangle.",
			"type": "array",
			"uniqueItems": true,
			"items": {
				"type": "array",
				"minItems": 3,
				"maxItems": 3,
				"items": {
					"type": "integer",
					"exclusiveMinimum": 0
				}
			}
		},
		"Medium": {
			"type": "string",
			"pattern": "FMedium::\\w+",
			"description": "The unique name of the medium."
		},
		"Color": {
			"type": "array",
			"description": "The RGB color code used to draw the surface.",
			"minItems": 3,
			"maxItems": 3,
			"items": {
				"type": "integer",
				"minimum": 0,
				"maximum": 255
			}
		}
	}
}